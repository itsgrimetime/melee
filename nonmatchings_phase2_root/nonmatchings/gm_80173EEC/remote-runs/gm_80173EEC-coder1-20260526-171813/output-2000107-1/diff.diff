--- before
+++ after
@@ -859,6 +859,7 @@
 void gm_80173EEC(void)
 {
   int i;
+  volatile unsigned long long new_var;
   u8 ckind;
   u16 *temp_r29;
   for (i = 0; i < 0x19; i++)
@@ -893,14 +894,16 @@
     if ((*temp_r29) >= 300)
     {
       ckind = gm_8016400C(i);
+      new_var = ckind == CKIND_ZELDA;
       fn_80172C78(gm_80160474(ckind, MJ_ALLSTAR));
-      if (ckind == CKIND_ZELDA)
+      if (new_var)
       {
         fn_80172C78(gm_80160474(CKIND_SEAK, MJ_ALLSTAR));
       }
+      i = CKIND_ZELDA;
       if (ckind == CKIND_SEAK)
       {
-        fn_80172C78(gm_80160474(CKIND_ZELDA, MJ_ALLSTAR));
+        fn_80172C78(gm_80160474(i, MJ_ALLSTAR));
       }
     }
   }
