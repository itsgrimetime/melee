--- before
+++ after
@@ -856,32 +856,38 @@
   return result;
 }
 
+inline int inline_fn(u8 arg0)
+{
+  return arg0 == CKIND_ZELDA;
+}
+
 void gm_80173EEC(void)
 {
   int i;
+  int new_var;
+  u16 *temp_r29;
+  new_var = fn_80164B48() != 0;
+  for (i = 0; i < 0x19; i++)
+  {
   u8 ckind;
-  u16 *temp_r29;
-  for (i = 0; i < 0x19; i++)
-  {
     temp_r29 = &gmMainLib_8015EDBC()->x18[i];
     if ((*temp_r29) >= 100)
     {
-      ckind = gm_8016400C(i);
-      fn_80172C78(gm_80160474(ckind, MJ_CLASSIC));
-      if (ckind == CKIND_ZELDA)
-      {
-        fn_80172C78(gm_80160474(CKIND_SEAK, MJ_CLASSIC));
+ do { ckind = gm_8016400C(i); fn_80172C78(gm_80160474(ckind, MJ_CLASSIC)); } while (0);
+      if (inline_fn(ckind))
+      {
       }
       if (ckind == CKIND_SEAK)
       {
         fn_80172C78(gm_80160474(CKIND_ZELDA, MJ_CLASSIC));
       }
     }
-    if ((*temp_r29) >= 200)
+    i = (*temp_r29) >= 200;
+    if (i)
     {
       ckind = gm_8016400C(i);
       fn_80172C78(gm_80160474(ckind, MJ_ADVENTURE));
-      if (ckind == CKIND_ZELDA)
+      if (inline_fn(ckind))
       {
         fn_80172C78(gm_80160474(CKIND_SEAK, MJ_ADVENTURE));
       }
@@ -894,18 +900,19 @@
     {
       ckind = gm_8016400C(i);
       fn_80172C78(gm_80160474(ckind, MJ_ALLSTAR));
-      if (ckind == CKIND_ZELDA)
+      if (inline_fn(ckind))
       {
         fn_80172C78(gm_80160474(CKIND_SEAK, MJ_ALLSTAR));
       }
       if (ckind == CKIND_SEAK)
       {
         fn_80172C78(gm_80160474(CKIND_ZELDA, MJ_ALLSTAR));
+        fn_80172C78(gm_80160474(CKIND_SEAK, MJ_CLASSIC));
       }
     }
   }
 
-  if (fn_80164B48() != 0)
+  if (new_var)
   {
     fn_80172C78(0xA0);
   }
