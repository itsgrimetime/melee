--- before
+++ after
@@ -859,10 +859,10 @@
 void gm_80173EEC(void)
 {
   int i;
-  u8 ckind;
   u16 *temp_r29;
   for (i = 0; i < 0x19; i++)
   {
+    u8 ckind;
     temp_r29 = &gmMainLib_8015EDBC()->x18[i];
     if ((*temp_r29) >= 100)
     {
@@ -896,11 +896,15 @@
       fn_80172C78(gm_80160474(ckind, MJ_ALLSTAR));
       if (ckind == CKIND_ZELDA)
       {
-        fn_80172C78(gm_80160474(CKIND_SEAK, MJ_ALLSTAR));
+        i = gm_80160474(CKIND_SEAK, MJ_ALLSTAR);
+        fn_80172C78(i);
       }
       if (ckind == CKIND_SEAK)
       {
         fn_80172C78(gm_80160474(CKIND_ZELDA, MJ_ALLSTAR));
+        if ((CKIND_ZELDA && CKIND_ZELDA) && CKIND_ZELDA)
+        {
+        }
       }
     }
   }
