# Generated by melee-agent debug permute config
# Pattern: (none detected — stock weights)

func_name = "lbDvd_80018A2C"
compiler_type = "mwcc"
objdump_command = "/opt/devkitpro/devkitPPC/bin/powerpc-eabi-objdump -dr -EB -mpowerpc -M broadway"

[scorer]
# Custom scorer (requires the [scorer] interface patch on
# the decomp-permuter side). When this section is present,
# permuter invokes `command <candidate.o>` per iteration
# and reads an integer score from stdout instead of
# running the built-in objdiff scorer.
command = "/home/discord/.local/bin/melee-agent debug target score-simplify-order --function lbDvd_80018A2C --target /home/discord/permuter-work/decomp-permuter/nonmatchings/lbDvd_80018A2C/simplify_order_target.yaml"
timeout_seconds = 5.0
