--- before
+++ after
@@ -859,6 +859,7 @@
 void gm_80173EEC(void)
 {
   int i;
+  volatile unsigned int new_var;
   u8 ckind;
   u16 *temp_r29;
   for (i = 0; i < 0x19; i++)
@@ -887,10 +888,10 @@
       }
       if (ckind == CKIND_SEAK)
       {
+      }
+    }
         fn_80172C78(gm_80160474(CKIND_ZELDA, MJ_ADVENTURE));
-      }
-    }
-    if ((*temp_r29) >= 300)
+    if (((*temp_r29) + 1) > 300)
     {
       ckind = gm_8016400C(i);
       fn_80172C78(gm_80160474(ckind, MJ_ALLSTAR));
@@ -900,12 +901,14 @@
       }
       if (ckind == CKIND_SEAK)
       {
-        fn_80172C78(gm_80160474(CKIND_ZELDA, MJ_ALLSTAR));
+        ckind = MJ_ALLSTAR;
+        fn_80172C78(gm_80160474(CKIND_ZELDA, (0, ckind)));
       }
     }
   }
 
-  if (fn_80164B48() != 0)
+  new_var = fn_80164B48() != 0;
+  if (new_var)
   {
     fn_80172C78(0xA0);
   }
