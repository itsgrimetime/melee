--- before
+++ after
@@ -856,14 +856,19 @@
   return result;
 }
 
+inline int inline_fn(int arg0)
+{
+  return arg0;
+}
+
 void gm_80173EEC(void)
 {
   int i;
-  u8 ckind;
+  volatile long long ckind;
   u16 *temp_r29;
   for (i = 0; i < 0x19; i++)
   {
-    temp_r29 = &gmMainLib_8015EDBC()->x18[i];
+    temp_r29 = &gmMainLib_8015EDBC()->x18[inline_fn(i)];
     if ((*temp_r29) >= 100)
     {
       ckind = gm_8016400C(i);
@@ -871,6 +876,8 @@
       if (ckind == CKIND_ZELDA)
       {
         fn_80172C78(gm_80160474(CKIND_SEAK, MJ_CLASSIC));
+        ckind = gm_8016400C(i);
+        ckind = ckind;
       }
       if (ckind == CKIND_SEAK)
       {
@@ -893,6 +900,9 @@
     if ((*temp_r29) >= 300)
     {
       ckind = gm_8016400C(i);
+      if (!CKIND_SEAK)
+      {
+      }
       fn_80172C78(gm_80160474(ckind, MJ_ALLSTAR));
       if (ckind == CKIND_ZELDA)
       {
