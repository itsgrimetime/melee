repos:
- repo: https://github.com/pre-commit/mirrors-clang-format
  rev: v18.1.8
  hooks:
  - id: clang-format

- repo: local
  hooks:
  - id: check-fork-files
    name: Check for fork-only files
    entry: python tools/check-fork-files.py
    language: python
    pass_filenames: false
    always_run: true
    description: Prevents fork-specific files from being committed to upstream PRs
