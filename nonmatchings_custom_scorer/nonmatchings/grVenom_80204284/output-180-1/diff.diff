--- before
+++ after
@@ -2283,32 +2283,39 @@
 Ground_GObj *grVenom_80203EAC(int gobj_id);
 void grVenom_80204284(Ground_GObj *gobj)
 {
+  s32 new_var4;
   Ground *gp;
-  HSD_JObj *src_jobj;
   HSD_JObj *dst_jobj;
   u32 pad2;
+  Ground *new_var3;
   Ground_GObj *other_gobj;
   Vec3 pos;
+  HSD_JObj *src_jobj;
   s32 timer;
+  HSD_JObj *new_var2;
   u32 pad;
+  Ground_GObj *new_var;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
   src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
-  dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
+  new_var3 = gp;
+  dst_jobj = (new_var2 = (HSD_JObj *) ((HSD_GObj *) new_var3->gv.venom.xC4)->hsd_obj);
   HSD_JObjGetTranslation(src_jobj, &pos);
   HSD_JObjSetTranslate(dst_jobj, &pos);
   Ground_801C39C0();
   Ground_801C3BB4();
-  timer = gp->gv.venom.xC8;
-  if (timer > 0)
+  timer = new_var3->gv.venom.xC8;
+  if ((timer > 0) & 0xFFFFFFFFFFFFFFFFu)
   {
     gp->gv.venom.xC8 = timer + 1;
-    if (((s32) gp->gv.venom.xC8) >= 0x3C)
-    {
-      if (((s32) gp->gv.venom.xC8) == 0x3C)
+    new_var4 = (s32) gp->gv.venom.xC8;
+    if (new_var4 >= 0x3C)
+    {
+      if (((s32) new_var3->gv.venom.xC8) == 0x3C)
       {
         ifStatus_802F6898();
         un_802FF570();
-        other_gobj = (Ground_GObj *) grVenom_80203EAC(1);
+        new_var = (Ground_GObj *) grVenom_80203EAC(1);
+        other_gobj = new_var;
         if (other_gobj != 0L)
         {
           Ground *other_gp = (Ground *) HSD_GObjGetUserData(other_gobj);
@@ -2330,6 +2337,9 @@
   }
   lb_800115F4();
   grVenom_8020362C();
+  if (!other_gobj)
+  {
+  }
   Ground_801C2FE0(gobj);
 }
 
