--- before
+++ after
@@ -2286,25 +2286,30 @@
   Ground *gp;
   HSD_JObj *src_jobj;
   HSD_JObj *dst_jobj;
+  HSD_JObj *new_var3;
   u32 pad2;
   Ground_GObj *other_gobj;
+  int new_var2;
   Vec3 pos;
+  volatile unsigned long new_var;
   s32 timer;
   u32 pad;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
   src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
-  dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
-  HSD_JObjGetTranslation(src_jobj, &pos);
+  new_var3 = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
+  dst_jobj = new_var3;
   HSD_JObjSetTranslate(dst_jobj, &pos);
   Ground_801C39C0();
   Ground_801C3BB4();
   timer = gp->gv.venom.xC8;
+  HSD_JObjGetTranslation(src_jobj, &pos);
+  new_var = ((s32) gp->gv.venom.xC8) == 0x3C;
   if (timer > 0)
   {
     gp->gv.venom.xC8 = timer + 1;
-    if (((s32) gp->gv.venom.xC8) >= 0x3C)
-    {
-      if (((s32) gp->gv.venom.xC8) == 0x3C)
+    if (new_var2 = ((s32) gp->gv.venom.xC8) >= 0x3C)
+    {
+      if (new_var)
       {
         ifStatus_802F6898();
         un_802FF570();
