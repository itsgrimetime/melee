--- before
+++ after
@@ -2286,22 +2286,28 @@
   Ground *gp;
   HSD_JObj *src_jobj;
   HSD_JObj *dst_jobj;
+  int new_var;
   u32 pad2;
   Ground_GObj *other_gobj;
   Vec3 pos;
+  HSD_JObj *new_var3;
+  void *new_var2;
   s32 timer;
   u32 pad;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
-  src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
+  new_var3 = (HSD_JObj *) (new_var2 = HSD_GObjGetHSDObj(gobj));
+  src_jobj = new_var3;
   dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
   HSD_JObjGetTranslation(src_jobj, &pos);
   HSD_JObjSetTranslate(dst_jobj, &pos);
   Ground_801C39C0();
   Ground_801C3BB4();
+  gp->gv.venom.xC8 = gp->gv.venom.xC8 + 1;
   timer = gp->gv.venom.xC8;
   if (timer > 0)
   {
-    gp->gv.venom.xC8 = timer + 1;
+    gp->gv.venom.xC8 = timer;
+    gp->gv.venom.xC8 = gp->gv.venom.xC8 + 1;
     if (((s32) gp->gv.venom.xC8) >= 0x3C)
     {
       if (((s32) gp->gv.venom.xC8) == 0x3C)
@@ -2321,9 +2327,10 @@
         un_802FF570();
         if (Ground_801C2BA4(1) == 0L)
         {
+          new_var = 1;
           ifStatus_802F68F0();
           un_802FF620();
-          gp->gv.venom.xC8 = -1;
+          gp->gv.venom.xC8 = -new_var;
         }
       }
     }
