--- before
+++ after
@@ -2281,8 +2281,19 @@
 };
 void grVenom_8020362C(void);
 Ground_GObj *grVenom_80203EAC(int gobj_id);
+inline struct grVenom_GroundVars inline_fn(Ground *arg0)
+{
+  return arg0->gv.venom;
+}
+
+inline s32 inline_fn2(struct grVenom_GroundVars arg0)
+{
+  return (s32) arg0.xC8;
+}
+
 void grVenom_80204284(Ground_GObj *gobj)
 {
+  Vec3 *new_var;
   Ground *gp;
   HSD_JObj *src_jobj;
   HSD_JObj *dst_jobj;
@@ -2293,18 +2304,19 @@
   u32 pad;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
   src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
-  dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
-  HSD_JObjGetTranslation(src_jobj, &pos);
-  HSD_JObjSetTranslate(dst_jobj, &pos);
+  dst_jobj = (HSD_JObj *) ((HSD_GObj *) inline_fn(gp).xC4)->hsd_obj;
+  new_var = &pos;
+  HSD_JObjGetTranslation(src_jobj, new_var);
+  HSD_JObjSetTranslate(dst_jobj, new_var);
   Ground_801C39C0();
   Ground_801C3BB4();
-  timer = gp->gv.venom.xC8;
+  timer = inline_fn(gp).xC8;
   if (timer > 0)
   {
     gp->gv.venom.xC8 = timer + 1;
-    if (((s32) gp->gv.venom.xC8) >= 0x3C)
-    {
-      if (((s32) gp->gv.venom.xC8) == 0x3C)
+    if (inline_fn2(inline_fn(gp)) >= 0x3C)
+    {
+      if (((s32) inline_fn(gp).xC8) == 0x3C)
       {
         ifStatus_802F6898();
         un_802FF570();
@@ -2318,13 +2330,7 @@
       else
       {
         ifStatus_802F6898();
-        un_802FF570();
-        if (Ground_801C2BA4(1) == 0L)
-        {
-          ifStatus_802F68F0();
-          un_802FF620();
-          gp->gv.venom.xC8 = -1;
-        }
+ do { un_802FF570(); if (Ground_801C2BA4(1) == 0L) { ifStatus_802F68F0(); un_802FF620(); gp->gv.venom.xC8 = -1; } } while (0);
       }
     }
   }
