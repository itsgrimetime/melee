--- before
+++ after
@@ -2283,28 +2283,36 @@
 Ground_GObj *grVenom_80203EAC(int gobj_id);
 void grVenom_80204284(Ground_GObj *gobj)
 {
+  Ground_GObj *new_var2;
+  void **new_var3;
   Ground *gp;
   HSD_JObj *src_jobj;
   HSD_JObj *dst_jobj;
   u32 pad2;
   Ground_GObj *other_gobj;
   Vec3 pos;
+  struct grVenom_GroundVars *new_var;
+  void *new_var5;
   s32 timer;
   u32 pad;
+  Ground *new_var4;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
+  new_var4 = gp;
+  new_var3 = &((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
   src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
-  dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
+  dst_jobj = (HSD_JObj *) (new_var5 = *new_var3);
   HSD_JObjGetTranslation(src_jobj, &pos);
   HSD_JObjSetTranslate(dst_jobj, &pos);
   Ground_801C39C0();
   Ground_801C3BB4();
-  timer = gp->gv.venom.xC8;
+  new_var = &gp->gv.venom;
+  timer = (*new_var).xC8;
   if (timer > 0)
   {
-    gp->gv.venom.xC8 = timer + 1;
-    if (((s32) gp->gv.venom.xC8) >= 0x3C)
-    {
-      if (((s32) gp->gv.venom.xC8) == 0x3C)
+    new_var4->gv.venom.xC8 = timer + 1;
+    if (((s32) (*new_var).xC8) >= 0x3C)
+    {
+      if (((s32) (*new_var).xC8) == 0x3C)
       {
         ifStatus_802F6898();
         un_802FF570();
@@ -2312,7 +2320,8 @@
         if (other_gobj != 0L)
         {
           Ground *other_gp = (Ground *) HSD_GObjGetUserData(other_gobj);
-          grCorneria_801E25C4(other_gobj, &other_gp->gv.venom.xC4, 4, 6, 0x6B6CC);
+          new_var2 = other_gobj;
+          grCorneria_801E25C4(new_var2, &other_gp->gv.venom.xC4, 4, 6, 0x6B6CC);
         }
       }
       else
