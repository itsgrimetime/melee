--- before
+++ after
@@ -2289,22 +2289,31 @@
   u32 pad2;
   Ground_GObj *other_gobj;
   Vec3 pos;
+  struct grVenom_GroundVars *new_var2;
+  Ground *new_var3;
+  Ground *new_var;
   s32 timer;
+  HSD_JObj *new_var5;
   u32 pad;
+  s32 new_var4;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
   src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
-  dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
+  new_var = gp;
+  new_var5 = (HSD_JObj *) ((HSD_GObj *) new_var->gv.venom.xC4)->hsd_obj;
+  dst_jobj = new_var5;
   HSD_JObjGetTranslation(src_jobj, &pos);
   HSD_JObjSetTranslate(dst_jobj, &pos);
   Ground_801C39C0();
   Ground_801C3BB4();
-  timer = gp->gv.venom.xC8;
-  if (timer > 0)
-  {
-    gp->gv.venom.xC8 = timer + 1;
-    if (((s32) gp->gv.venom.xC8) >= 0x3C)
-    {
-      if (((s32) gp->gv.venom.xC8) == 0x3C)
+  new_var2 = &new_var->gv.venom;
+  timer = (*new_var2).xC8;
+  if (0 < timer)
+  {
+    new_var->gv.venom.xC8 = timer + 1;
+    if (((s32) (*new_var2).xC8) >= 0x3C)
+    {
+      new_var4 = (s32) new_var->gv.venom.xC8;
+      if (new_var4 == 0x3C)
       {
         ifStatus_802F6898();
         un_802FF570();
@@ -2322,9 +2331,8 @@
         if (Ground_801C2BA4(1) == 0L)
         {
           ifStatus_802F68F0();
-          un_802FF620();
-          gp->gv.venom.xC8 = -1;
-        }
+          new_var3 = new_var;
+ un_802FF620(); new_var3->gv.venom.xC8 = -1; }
       }
     }
   }
