--- before
+++ after
@@ -2286,39 +2286,49 @@
   Ground *gp;
   HSD_JObj *src_jobj;
   HSD_JObj *dst_jobj;
+  HSD_JObj *new_var2;
+  volatile unsigned char new_var;
+  HSD_GObj *new_var5;
   u32 pad2;
-  Ground_GObj *other_gobj;
   Vec3 pos;
-  s32 timer;
+  volatile unsigned int timer;
   u32 pad;
+  Ground_GObj *new_var3;
+  u32 new_var4;
   gp = (Ground *) HSD_GObjGetUserData(gobj);
   src_jobj = (HSD_JObj *) HSD_GObjGetHSDObj(gobj);
-  dst_jobj = (HSD_JObj *) ((HSD_GObj *) gp->gv.venom.xC4)->hsd_obj;
-  HSD_JObjGetTranslation(src_jobj, &pos);
-  HSD_JObjSetTranslate(dst_jobj, &pos);
+  new_var5 = (HSD_GObj *) gp->gv.venom.xC4;
+  ;
+  HSD_JObjGetTranslation(new_var2 = src_jobj, &pos);
+  HSD_JObjSetTranslate((HSD_JObj *) new_var5->hsd_obj, &pos);
+  pad = gp->gv.venom.xC8;
   Ground_801C39C0();
   Ground_801C3BB4();
-  timer = gp->gv.venom.xC8;
+  timer = pad;
   if (timer > 0)
   {
-    gp->gv.venom.xC8 = timer + 1;
-    if (((s32) gp->gv.venom.xC8) >= 0x3C)
-    {
-      if (((s32) gp->gv.venom.xC8) == 0x3C)
-      {
+    gp->gv.venom.xC8 = ((((timer & 0xFFu) & 0xFFu) & 0xFFu) & 0xFFu) + 1;
+    new_var4 = gp->gv.venom.xC8;
+    if (((s32) new_var4) >= 0x3C)
+    {
+      new_var = 6;
+      if (((s32) pad) == 0x3C)
+      {
+        Ground_GObj *other_gobj;
         ifStatus_802F6898();
         un_802FF570();
-        other_gobj = (Ground_GObj *) grVenom_80203EAC(1);
+        new_var3 = (Ground_GObj *) grVenom_80203EAC(1);
+        other_gobj = new_var3;
         if (other_gobj != 0L)
         {
           Ground *other_gp = (Ground *) HSD_GObjGetUserData(other_gobj);
-          grCorneria_801E25C4(other_gobj, &other_gp->gv.venom.xC4, 4, 6, 0x6B6CC);
+          grCorneria_801E25C4(other_gobj, &other_gp->gv.venom.xC4, 4, new_var, 0x6B6CC);
         }
+        un_802FF570();
       }
       else
       {
         ifStatus_802F6898();
-        un_802FF570();
         if (Ground_801C2BA4(1) == 0L)
         {
           ifStatus_802F68F0();
